// vue-i18n options for @nuxtjs/i18n. Locale messages themselves are loaded from
// the per-locale JSON files declared in nuxt.config (i18n.locales[].file).
export default defineI18nConfig(() => ({
  legacy: false,
  fallbackLocale: "en",
}));
